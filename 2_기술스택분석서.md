# 2. 기술 스택 분석서 — 오시마켓 (Oshi Market)

MVP 스코프([`1_MVP기획서.md`](./1_MVP기획서.md)) 기준으로 정한 기술 스택.

## 확정 항목

| 영역 | 선택 | 사유 |
|---|---|---|
| 백엔드 언어/프레임워크 | Java 17 + Spring Boot 4.0.8 | 팀 학습 경험 기반, LTS 버전으로 안정성 확보 |
| DB | PostgreSQL 16 | MySQL은 그동안 충분히 다뤄봐서, 팀 학습 목적으로 전환. Elasticsearch 도입 전까지 PostgreSQL 내장 풀텍스트 검색(`tsvector`)으로 검색 기능을 보완할 수 있다는 실질적 이점도 있음 |
| 프론트엔드 | React | 컴포넌트 재사용 + state 기반 자동 리렌더링이 순수 HTML보다 유지보수에 유리 (상세 근거는 아래 선택 이유 요약 참고) |

## 추가 제안 항목

| 영역 | 선택 | 사유 |
|---|---|---|
| 빌드 툴 | Gradle | Maven 대비 설정이 간결하고 최신 Spring 프로젝트의 사실상 표준 |
| 인증 방식 | JWT (Spring Security) | 무상태(stateless) 인증으로 API 서버에 적합, 팀 기학습 분야 |
| 실시간 통신 | WebSocket + STOMP | [`1_MVP기획서.md`](./1_MVP기획서.md)에서 확정한 실시간 채팅(담당: yjdev101) 구현 기반 |
| ORM/영속성 | Spring Data JPA + Hibernate | Spring Boot 표준 조합, PostgreSQL 포함 주요 RDB와 궁합 좋음 |
| API 문서화 | SpringDoc OpenAPI (Swagger) | React 프론트와 API 계약 공유·검증에 필수 |
| 테스트 | JUnit5 + Mockito | Spring Boot 기본 번들, 별도 설정 부담 없음 |
| 버전 관리 | Git + GitHub | 이미 사용 중 |
| 모니터링 | Spring Boot Actuator + Prometheus + Grafana | Actuator가 헬스/메트릭을 내장 제공, Docker Compose로 EC2에 함께 띄우면 추가 인프라 비용 없이 대시보드 확보 |
| 부하 테스트 | k6 | Grafana Labs 제품이라 Prometheus+Grafana 모니터링 스택과 네이티브 연동, 스크립트가 JS라 React에서 쓰는 언어 그대로 재사용, .js라 git으로 버전 관리·리뷰 용이 |

## 배포 환경

| 영역 | 선택 | 사유 |
|---|---|---|
| 배포 환경 | Docker + AWS EC2 | 실제 배포 경험 확보, URL로 언제든 시연 가능 (프리티어 활용) |

로컬에서 Docker Compose로 먼저 검증 후 EC2에 배포하는 순서로 진행.

## 선택 이유 요약

| 기술 | 선택 이유 |
|---|---|
| Java 17 | 21의 핵심 차별점인 Virtual Threads는 이 프로젝트 규모에서 체감 이점이 없고 pinning 리스크만 추가되며, Spring Boot 4.0.8 최소 요구 버전이자 21보다 자료가 성숙해 막혔을 때 참고할 게 많음 |
| PostgreSQL | 선생님 피드백으로 전환(2026-09-16) — MySQL은 팀이 이미 충분히 경험해서 학습 목적으로 선택. FK 관계가 명확한 정형 데이터(User-Item-Transaction) 자체는 MySQL과 동일하게 잘 맞고, 추가로 `tsvector` 기반 내장 풀텍스트 검색을 Elasticsearch 도입 전 중간 단계로 활용 가능 |
| React | 순수 HTML은 상품 카드 마크업(목록·검색·찜 등 여러 화면에 반복)을 페이지마다 복붙해야 하고, 찜 여부·새 메시지 같은 상태가 바뀔 때마다 `document.querySelector`로 DOM을 직접 찾아 고쳐야 해 화면 간 동기화 버그가 잦음. React는 `ItemCard` 같은 컴포넌트를 props로 재사용하고, state 하나가 바뀌면 그걸 쓰는 화면이 자동으로 다시 그려져 이 문제를 구조적으로 없애고, 페이지 이동도 전체 리로드 없이 처리돼 채팅창 같은 상태를 유지할 수 있음 |
| Gradle | Maven보다 설정이 간결하고 최신 Spring 프로젝트의 사실상 표준 |
| JWT | Stateless라 서버 확장에 유리하고 SPA(React) 클라이언트와 궁합이 좋음 |
| WebSocket + STOMP | 채팅의 실시간 양방향성에 폴링보다 서버 부하·지연이 적음 |
| Spring Data JPA | 반복 CRUD 쿼리를 줄여 MVP 개발 속도 확보 |
| Docker + AWS EC2 | 로컬-운영 환경 일치, 프리티어로 실배포 경험 확보 |
| Prometheus + Grafana | 배포 후 트래픽/응답시간/에러율을 실시간으로 보고, 과부하 테스트 때 병목 구간을 눈으로 확인하기 위함 |
| k6 | 배포한 EC2 서버에 동시 요청을 걸어 몇 명까지 버티는지, 어디서 응답 지연이 시작되는지 확인하기 위함. Grafana와 네이티브 연동돼 모니터링 대시보드에서 바로 결과 확인 가능 |

## 향후 확장 계획 (확정, 시점 미정)

| 항목 | 계획 | 사유 |
|---|---|---|
| 검색 | Elasticsearch 도입 | 선생님 피드백(2026-09-16) — 지금은 카테고리/태그 필터 수준이라 PostgreSQL 인덱스·풀텍스트로 충분하지만, 자유 텍스트 검색·오타 허용 등 검색 품질을 높이려면 Elasticsearch로 전환 예정. MVP 이후 일정에 맞춰 착수 |
| 채팅 데이터 저장소 | MongoDB/Redis 검토 | 채팅 메시지가 쓰기 폭주 트래픽 특성이라 관계형 DB보다 유리할 수 있음. k6 부하 테스트로 PostgreSQL의 실제 한계 지점을 먼저 확인한 뒤 필요성 판단 |

## 인증/실시간 통신 흐름 요약

- REST API: JWT Access Token을 `Authorization: Bearer` 헤더로 전달
- WebSocket(채팅): 연결 시 JWT로 핸드셰이크 인증, STOMP 세션 유지

상세 아키텍처와 서비스 흐름은 [`3_아키텍처및서비스흐름.md`](./3_아키텍처및서비스흐름.md)에서 다룸.
