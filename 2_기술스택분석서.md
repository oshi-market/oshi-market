# 2. 기술 스택 분석서 — 오시마켓 (Oshi Market)

MVP 스코프([`1_MVP기획서.md`](./1_MVP기획서.md)) 기준으로 정한 기술 스택.

## 확정 항목

| 영역 | 선택 | 사유 |
|---|---|---|
| 백엔드 언어/프레임워크 | Java 17 + Spring Boot 3.x | 팀 학습 경험 기반, LTS 버전으로 안정성 확보 |
| DB | MySQL 8.0 | 팀이 계속 사용해온 스택, 학습 곡선 최소화 |
| 프론트엔드 | React | 확정 |

## 추가 제안 항목

| 영역 | 선택 | 사유 |
|---|---|---|
| 빌드 툴 | Gradle | Maven 대비 설정이 간결하고 최신 Spring 프로젝트의 사실상 표준 |
| 인증 방식 | JWT (Spring Security) | 무상태(stateless) 인증으로 API 서버에 적합, 팀 기학습 분야 |
| 실시간 통신 | WebSocket + STOMP | [`1_MVP기획서.md`](./1_MVP기획서.md)에서 확정한 실시간 채팅(담당: yjdev101) 구현 기반 |
| ORM/영속성 | Spring Data JPA + Hibernate | Spring Boot 표준 조합, MySQL과 궁합 좋음 |
| API 문서화 | SpringDoc OpenAPI (Swagger) | React 프론트와 API 계약 공유·검증에 필수 |
| 테스트 | JUnit5 + Mockito | Spring Boot 기본 번들, 별도 설정 부담 없음 |
| 버전 관리 | Git + GitHub | 이미 사용 중 |

## 배포 환경

| 영역 | 선택 | 사유 |
|---|---|---|
| 배포 환경 | Docker + AWS EC2 | 실제 배포 경험 확보, URL로 언제든 시연 가능 (프리티어 활용) |

로컬에서 Docker Compose로 먼저 검증 후 EC2에 배포하는 순서로 진행.

## 선택 이유 (기술 면접 대비)

면접에서 "왜 이 기술을 선택했나요?"에 대한 답변은 **선택한 이유 + 고려한 대안 + 트레이드오프** 구조로 준비.

**Java 17 + Spring Boot 3.x**
- 이유: 정적 타이핑으로 대규모 협업 시 컴파일 단계에서 오류를 잡을 수 있고, Spring의 DI/AOP로 관심사 분리가 쉬워 유지보수성이 높음. 팀이 이미 학습한 스택이라 러닝커브 없이 바로 생산성 확보 가능
- 대안: Node.js(Express) — 개발 속도는 빠르지만 타입 안정성이 약하고, 팀 학습 경험이 없어 MVP 기간 내 리스크가 큼
- **왜 21이 아니라 17인가**: 21도 LTS라 "LTS라서"는 이유가 안 됨. 21의 핵심 기능인 Virtual Threads는 대량 동시 I/O 처리에 강점이 있지만, 이 프로젝트 규모(2인 MVP, 소규모 트래픽)에서는 체감 이점이 거의 없고 오히려 blocking JDBC와 virtual thread pinning 같은 알려진 이슈로 디버깅 리스크만 늘 수 있음. 반대로 17은 더 오래 LTS였던 만큼 Spring Boot·JPA 라이브러리, 튜토리얼, StackOverflow 자료가 더 성숙해 있어 제한된 세션 시간 안에 막혔을 때 참고할 자료가 많다는 실질적 이점이 있음

**MySQL**
- 이유: User–Item–Transaction처럼 FK 관계가 명확한 정형 데이터 구조에 적합, AWS RDS 등 클라우드 호환성이 좋고 팀이 이미 다뤄본 DB라 설계·튜닝에 시간을 아낄 수 있음
- 대안: PostgreSQL — 풀텍스트 검색·JSONB 등 강점이 있지만, MVP 단계의 검색은 태그/카테고리 필터 수준이라 이점이 크지 않고 학습 비용만 늘어남

**React**
- 이유: ① 상품 카드형 UI(목록/검색/큐레이션)가 반복되는 컴포넌트 구조라 React의 component+props 모델과 직접 맞음 ② 실시간 채팅(WebSocket) 상태를 화면에 즉시 반영해야 하는데, React의 state 기반 리렌더링이 수동 DOM 조작보다 관리가 쉬움 ③ 상품탐색→상세→거래요청까지 페이지 전체 리로드 없이 이어지는 SPA 흐름이 사용자 경험(특히 채팅 유지)에 중요
- 대안: Vue — 러닝커브는 낮지만 위 세 가지 요구사항(반복 컴포넌트, 실시간 상태 반영, SPA 흐름)을 충족하는 데는 React와 동등하며, 팀이 이미 React로 방향을 정해 전환 비용을 들일 이유가 없음

**JWT (Spring Security)**
- 이유: Stateless 인증이라 서버에 세션을 저장하지 않아도 되어 확장성(스케일아웃)에 유리하고, SPA(React) 클라이언트와 궁합이 좋음
- 대안: 세션 기반 인증 — 구현은 단순하지만 서버가 상태를 들고 있어야 해 확장성이 떨어짐

**WebSocket + STOMP**
- 이유: 채팅처럼 양방향 실시간성이 필요한 기능에 폴링 대비 서버 부하가 적고 지연이 낮음. STOMP로 pub/sub 메시징 구조를 표준화해 구현 복잡도를 낮춤
- 대안: 폴링/롱폴링 — 구현은 쉽지만 요청량이 많아지고 실시간성이 떨어져 채팅 UX에 부적합

**Spring Data JPA + Hibernate**
- 이유: 반복적인 CRUD 쿼리를 줄여 생산성을 높이고, Entity 중심 설계가 Spring 생태계와 자연스럽게 통합됨
- 대안: MyBatis — SQL을 세밀하게 제어할 수 있지만 보일러플레이트가 많아 MVP 개발 속도에는 불리

**Docker + AWS EC2**
- 이유: 로컬–운영 환경 일관성을 보장하고, 컨테이너화로 이식성을 확보. EC2 프리티어로 비용 부담 없이 실제 배포·운영 경험을 쌓을 수 있어 이후 포트폴리오·면접에서 "배포까지 직접 했다"는 근거가 됨
- 대안: Heroku/Vercel 같은 PaaS — 배포는 간편하지만 인프라 이해도(네트워크, 컨테이너 오케스트레이션)를 보여주기 어려워 학습 목적에는 EC2+Docker가 더 적합

## 인증/실시간 통신 흐름 요약

- REST API: JWT Access Token을 `Authorization: Bearer` 헤더로 전달
- WebSocket(채팅): 연결 시 JWT로 핸드셰이크 인증, STOMP 세션 유지

상세 아키텍처와 서비스 흐름은 [`3_아키텍처및서비스흐름.md`](./3_아키텍처및서비스흐름.md)에서 다룸.
